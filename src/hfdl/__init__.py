"""HuggingFace Simple Downloader.

The version lives here and nowhere else: pyproject.toml reads it through
hatchling, the window title reads it at startup, and .github/workflows/release.yml
refuses a tag that disagrees with it.
"""

__version__ = "1.3.1"
